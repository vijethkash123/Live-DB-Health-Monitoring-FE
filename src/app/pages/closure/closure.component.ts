import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'closure-cmp',
    moduleId: module.id,
    templateUrl: 'closure.component.html'
})

export class ClosureComponent implements OnInit{
    ngOnInit(){
    }
}
